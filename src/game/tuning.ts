import type { FoeKind } from "./types";

/** Все числа баланса в одном месте - крутить их придётся часто. */
/**
 * Видимая область в логических пикселях.
 *
 * Высота фиксирована геометрией мира: платформы начинаются с y=26,
 * земля на y=62 и имеет толщину 15 - ниже 77 не ужать, обрежется уровень.
 *
 * Ширина подстраивается под экран. Мир по своей природе широкий (2.86:1),
 * и на вертикальном телефоне такой кадр вырождается в узкую полоску
 * высотой 16% экрана. Сужая кадр, мы делаем полоску выше, а спрайты
 * крупнее - ценой обзора вперёд, поэтому есть нижняя граница.
 */
/**
 * Сколько раз в секунду считается физика.
 *
 * Раньше шаг делался на каждый кадр монитора, и игра шла со скоростью экрана:
 * на 60 Гц один темп, на 120 Гц вдвое быстрее, на 144 Гц втрое. Один и тот же
 * уровень на маке и на винде проходился за разное время, а таблица рекордов
 * из-за этого была бессмысленной.
 *
 * Теперь темп задаётся здесь и одинаков везде. Числа баланса подобраны под
 * шаг, поэтому менять стоит только это значение: 60 - как было на маке,
 * 120 - как на быстром мониторе.
 */
export const TICKS_PER_SECOND = 75;

export const VIEW = { w: 220, h: 112 };

/**
 * Плотность точек холста. Ставится при подгонке размера под экран и
 * учитывается в матрице отрисовки: без этого на ретине сглаженные края
 * получаются мыльными, потому что холст вдвое ниже разрешением, чем экран.
 */
export const RENDER = { density: 1 };

/** Ниже этого обзора вперёд не хватает, чтобы среагировать на врага. */
export const VIEW_W_MIN = 150;
/** Проектная ширина кадра: столько обзора закладывалось при рисовании карт. */
export const VIEW_W_MAX = 220;

/**
 * Рост персонажа. Джун маленький, с первого же оффера становится выше -
 * и с этого момента ему нужен больший зазор под платформами.
 *
 * Отсюда правило для карт: низ любой платформы, под которой надо пройти,
 * обязан быть выше макушки большого игрока. Земля 62, большой 15 высотой,
 * значит макушка на 47, а платформа не ниже y=43.
 */
/**
 * Вся геометрия считается от роста маленького игрока - это базовая единица
 * жанра, как рост Марио. Канонические пропорции:
 *
 *   большой игрок      2U      блок               1U
 *   высота прыжка      4U      длина прыжка       5U
 *   низ блока над землёй 3U    толщина земли      2U
 *
 * Отсюда и высота кадра: чтобы над землёй помещалось ~8 ростов, а не 5.6,
 * иначе блоки приходится вешать над самой головой и прыжок некуда девать.
 */
export const UNIT = 11;

export const PLAYER_W = 9;
export const PLAYER_H_SMALL = 11;
export const PLAYER_H_BIG = 22;

/** Самая низкая платформа, под которой большой игрок ещё пролезает. */
export const MAX_PLATFORM_Y = 64;

/**
 * Цепочка растоптанных без касания земли. Классика жанра: одиночный прыжок
 * на врага стоит немного, а вот пять подряд - уже событие. Дальше таблицы
 * идёт жизнь: за красивую игру платят не очками, а попыткой.
 */
export const COMBO_SCORE = [200, 400, 800, 1000, 2000, 4000] as const;

/** Каждые столько собранных скиллов дают жизнь. На всех картах их около 600. */
export const SKILLS_PER_LIFE = 100;

/** Сколько скиллов отдаёт кирпич-заначка, прежде чем опустеть. */
export const COIN_HITS = 10;

/**
 * Жизни на старте каждого уровня. Пройденный уровень восполняет их до
 * startLives; лишние, заработанные сверху, переносятся, но не больше
 * этого числа - иначе к последнему миру копилка из двадцати жизней
 * превращает игру в прогулку, а выгорание перестаёт что-то значить.
 */
export const MAX_CARRY_LIVES = 5;

/** Кадры заставки перед уровнем. Пауза на вдох, а не экран загрузки. */
export const INTRO_FRAMES = 105;

/** Флагшток оффера перед дверью: высота и максимум за верхушку. */
export const POLE_H = 44;
export const POLE_BONUS_MAX = 3000;

/** Шаг между звеньями крутящейся ротации алертов. */
export const ROTOR_STEP = 7;

/**
 * Размеры обязаны совпадать с тем, что рисуют спрайты: иначе враг тонет в
 * земле или парит над ней. Сторожит check:sprites, он же покажет, если
 * кто-то поправит рисунок и забудет про хитбокс.
 */
export const FOE_SIZE: Record<FoeKind, { w: number; h: number; speed: number }> = {
  legacy: { w: 12, h: 10, speed: 0.3 },
  bug: { w: 9, h: 8, speed: 0.72 },
  call: { w: 13, h: 10, speed: 0.42 },
  // Техдолг тяжёлый и медленный: его видно издалека, и обойти проще,
  // чем сносить. Ростом с маленького игрока, но вдвое шире.
  debt: { w: 16, h: 12, speed: 0.22 },
  // Рекрутёр парит, поэтому высота считается от baseY как у созвона.
  hr: { w: 11, h: 9, speed: 0.5 },
};

/** Кто парит в воздухе, а не ходит по земле. */
export const FLYING_FOES: Record<FoeKind, boolean> = {
  legacy: false, bug: false, call: true, debt: false, hr: true,
};

export const TUNING = {
  scale: 4,

  accel: 0.5,
  friction: 0.8,
  gravity: 0.23,
  maxFall: 6,
  /**
   * Подъём 48px: ноги в верхней точке на 42, а верх стандартного ряда
   * ящиков на 45. При -4.5 подъём был 44 и до верха ряда не хватало одного
   * пикселя - на ящики нельзя было запрыгнуть вовсе, только бить снизу.
   * В классике на ряд ящиков запрыгивают с разбега, это часть жанра.
   */
  jumpImpulse: -4.7,
  /** Отпустил прыжок - скорость подъёма срезается до этого. Даёт прыжок по длительности. */
  jumpCut: -1.4,
  /** Кадры прощения после схода с платформы. Без этого игра ощущается нечестной. */
  coyoteFrames: 7,
  /** Кадры, в которые засчитается прыжок, нажатый до приземления. */
  bufferFrames: 7,

  stompBounce: -3.2,
  /** Насколько глубоко надо быть выше врага, чтобы это считалось растаптыванием. */
  stompTolerance: 0.75,
  stompMinFallSpeed: 0.9,

  hurtFrames: 64,
  knockbackX: 2.6,
  knockbackY: -2.4,

  coffeeFrames: 480,
  coffeeMultiplier: 1.55,
  swampMultiplier: 0.5,
  swampJumpMultiplier: 0.82,

  cameraEase: 0.11,
  shakeFrames: 8,

  /** Отпуск: неуязвимость примерно на 9 секунд. */
  vacationFrames: 540,
  /** Пауза между брошенными тестами. */
  throwCooldown: 22,
  throwSpeed: 3.2,
  throwLift: -2.2,
  /** Тест живёт около 2 секунд, потом гаснет. */
  throwLife: 130,
  throwBounce: -3.4,

  startLives: 3,
  scoreGem: 100,
  scoreCoffee: 50,
  scoreStomp: 200,
  scoreLevelClear: 500,
  scoreLifeBonus: 250,
  scoreTested: 150,
  scoreVacation: 200,
  scoreBoss: 2000,
  /**
   * Бонус за скорость на финише уровня. Время обязано на что-то влиять,
   * иначе его незачем и показывать. Отсчёт от нормы уровня (LevelSpec.par):
   * уложился - получил остаток и звезду, не уложился - просто ноль.
   */
  scorePerSecondLeft: 12,
  /**
   * С этой секунды таймер краснеет, а музыка ускоряется. Штрафа за нуль нет:
   * игру открывают из чата на пять минут, и убивать за медленную игру там,
   * где и так есть стена дедлайна, - лишняя жестокость.
   */
  hurrySeconds: 15,
} as const;
